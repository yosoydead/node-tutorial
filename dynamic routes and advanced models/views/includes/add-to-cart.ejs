<form action="/cart" method="POST">
    <button class='btn' type="submit">Add to cart</button>

    <!-- this hidden input stores the id of the current product and keeps it in the request body
    so that i can use it later. this only works for POST requests -->
    <input type="hidden" name="productId" value="<%= product.id %>">
</form>