//- it works differently than normal html
//- seems like it doesnt use any < > tags
//- also, indentation matters, just like in python

<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title #{docTitle}
        link(rel="stylesheet", href="/css/main.css")

        //-use this if within a page you want to add more things to the head tag
        block styles
    body
        //-this creates a header tag
        //-classes are passed like <tag used>.<name of the class>
        //-this will be just like the navbar from the html shop
        header.main-header
            nav.main-header__nav
                ul.main-header__item-list
                    //- this is just one list element
                    li.main-header__item
                        a(href="/", class= (path === "/" ? "active" : "")) Shop
                    li.main-header__item
                        //-in that class, it says if the path is === to whatever path you want, set a class,
                        //-if not, dont set any class
                        a(href="admin/add-product", class=( path === "/admin/add-product" ? "active" : "")) Add Product
        
        //-insert here all the content of different pages that follow the same structure as above
        block content
        